[package]
name = "jpeg-cpai"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
jpeg-core = { path = "../core", default-features = false }

[features]
default = ["std"]
std = ["jpeg-core/std"]
